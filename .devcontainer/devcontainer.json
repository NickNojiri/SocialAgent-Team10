{
    "name": "SocialAgent-Team10",
    "postCreateCommand": "curl -fsSL https://ollama.com/install.sh | sh && pip install -r requirements.txt",
    "customizations": {
        "vscode": {
            "extensions": ["ms-python.python"]
        }
    }
}